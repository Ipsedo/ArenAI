# ArenAI : Game

This is the module that contains the game with GUI. You need a trained model to run it.

## Usage

After building with root cmake project, execute the binary :

```bash
cd /path/to/ArenAI
./build/arenai_desktop/arenai_desktop
```
